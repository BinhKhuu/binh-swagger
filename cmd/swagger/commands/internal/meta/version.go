package meta

/*
When this project grows I want the version command to print via build-time variables injected via -ldflags

    version
    commit hash
    build date
    OS/arch
*/

const Version = "0.1.0"
